# デプロイ手順(deployフォルダ)

`deploy` フォルダは、このアプリを実際にインターネット上に公開するための、そのまま使える一式です
(Vite + React構成)。手元のパソコンに Node.js がインストールされていれば、以下の手順で
ビルド・公開ができます。

## 1. フォルダを用意する

`deploy` フォルダの中身を、そのまま自分のプロジェクトフォルダとして使ってください。

```
deploy/
  index.html
  package.json
  vite.config.js
  public/
    manifest.json
    service-worker.js
    icons/
      icon-192.png
      icon-512.png
      icon-maskable-512.png
  src/
    main.jsx
    App.jsx      ← アプリ本体(このChatで作ってきたコード)
```

## 2. 動作確認(手元のパソコンで)

```bash
cd deploy
npm install
npm run dev
```

表示されるURL(例: http://localhost:5173)をブラウザで開いて、いつも通り動くか確認してください。

## 3. ビルドする

```bash
npm run build
```

`dist` フォルダが作られます。これがそのまま公開用のファイル一式です。

## 4. GitHub Pagesで公開する

### やり方A: gh-pagesパッケージを使う(簡単)

```bash
npm install --save-dev gh-pages
```

`package.json` の `"scripts"` に以下を追加:

```json
"deploy": "vite build && gh-pages -d dist"
```

`vite.config.js` の `base` を、実際のリポジトリ名に合わせて書き換える
(例: リポジトリ名が `chorus-db` なら `base: '/chorus-db/'`)。

その後:

```bash
npm run deploy
```

これで `dist` の中身が `gh-pages` ブランチに公開され、
`https://ユーザー名.github.io/リポジトリ名/` でアクセスできるようになります
(GitHubリポジトリの Settings → Pages で、公開元が `gh-pages` ブランチに
なっていることを確認してください)。

### やり方B: GitHub Actionsで自動化する

`main` ブランチにpushするたびに自動でビルド・公開したい場合は、GitHub Actionsの
「Deploy static content to Pages」テンプレートを使うのがおすすめです
(リポジトリの Actions タブから設定できます)。

## 5. Googleログイン(ドライブ保存)を使う場合の設定(任意)

このアプリはログインしなくても、これまで通りIndexedDB(端末内)だけで動きます。
Googleログインを有効にして、自分のGoogleドライブにもデータを保存したい場合は、
以下の設定が必要です。

### 5-1. Google Cloud Consoleでの準備

1. [Google Cloud Console](https://console.cloud.google.com/) で新しいプロジェクトを作成(または既存のものを使用)
2. 「APIとサービス」→「ライブラリ」で **Google Drive API** を有効化する
3. 「APIとサービス」→「OAuth同意画面」を設定する(External/Internalはご自身のGoogleアカウントの種類に合わせる。テスト段階では「テストユーザー」に自分のGoogleアカウントを追加すればよい)
4. 「APIとサービス」→「認証情報」→「認証情報を作成」→「OAuthクライアントID」
   - アプリケーションの種類: **ウェブアプリケーション**
   - 「承認済みのJavaScript生成元」に、実際に公開するURLを追加する
     - 例: `https://ユーザー名.github.io`(GitHub Pagesの場合、パス部分は不要でドメインのみ)
     - ローカルで動作確認する場合は `http://localhost:5173` も追加しておくと便利
   - リダイレクトURIの設定は不要(トークン取得はポップアップ方式のため)
5. 発行された「クライアントID」(`〜.apps.googleusercontent.com`という形式)をコピーする

### 5-2. アプリ側の設定

`src/App.jsx` 内の以下の行を、発行されたクライアントIDに書き換える:

```js
const GOOGLE_CLIENT_ID = 'YOUR_GOOGLE_OAUTH_CLIENT_ID.apps.googleusercontent.com';
```

このプレースホルダのままだと、ログインボタン自体が表示されず、
従来通りIndexedDBのみで動作します(壊れることはありません)。

### 5-3. 保存先について

- スコープは `drive.appdata`(個人データの自動保存用)と `drive.file`(共有時に作成する
  ファイル用、6章参照)の2つを要求します。`drive.appdata` は**Googleドライブの
  「アプリ専用の隠し領域(appDataFolder)」** で、ユーザー本人にもドライブの画面上には
  表示されず、他のファイルとは完全に分離されています。`drive.file` は、このアプリが
  作成したファイルだけにアクセスできるスコープで、ユーザーの他のドライブファイルには
  一切触れません。
- ログイン後、`drive.appdata`側に `chorus-data.json` という1つのファイルとしてデータ全体
  (プロフィール・曲一覧)が保存されます。
- Googleアカウント1つにつき、プロフィールは1つです(複数プロフィールの作成・切替は、
  ログインしていない状態でのみ、動作確認用の機能として残しています)。
- ログイン状態はアクセストークン(1時間程度で失効)をメモリ上に保持するだけで、
  トークン自体を保存することはありません。再読み込み時は、以前ログインしていた場合
  のみ自動的に再ログインを試みます(Googleの側でセッションが切れている場合は再度
  ログイン操作が必要です)。

## 6. 曲の大量共有(Googleドライブ経由)を使う場合の追加設定(任意)

「曲リストを共有」でQRコードに収まらないほど曲数が多いとき、曲データそのものをQR/URLに
埋め込む代わりに、Googleドライブ上に共有用ファイルを自動作成し、そのファイルへの短い
リンクだけをQRコード/URLにする機能です。ログインが前提の機能で、以下の追加設定をしないと
「Googleドライブ経由」のボタン自体が出ません(未設定でも他の機能には影響しません)。

### 6-1. APIキーの発行

1. Google Cloud Consoleの「APIとサービス」→「認証情報」→「認証情報を作成」→「APIキー」
2. 発行されたキーの「APIの制限」で **Google Drive API のみ** に制限することを推奨
3. 「アプリケーションの制限」で「ウェブサイト」を選び、公開ドメインを登録するとより安全

### 6-2. アプリ側の設定

`src/App.jsx` 内の以下の行を、発行されたAPIキーに書き換える:

```js
const GOOGLE_API_KEY = 'YOUR_GOOGLE_API_KEY';
```

### 6-3. 仕組みと共有範囲について

- 「曲リストを共有」→「Googleドライブ経由」を選ぶと、選んだ曲のデータをJSONファイルとして
  ユーザー自身の**通常のドライブ領域**(隠し領域ではない、マイドライブ直下)に新規作成し、
  そのファイルだけに「リンクを知っている全員が閲覧可」の権限をアプリが自動で設定します。
  ユーザーがGoogleドライブの画面を開いて手動で共有設定をする必要はありません。
- 作成されるファイルは共有のたびに新規作成されるため、他の曲データや過去の共有ファイルが
  影響を受けることはありません。ファイルはユーザーのドライブに残るので、不要になったものは
  ドライブの画面から手動で削除できます(アプリ側からの一括削除機能は未実装です)。
- 受け取る側はGoogleアカウントを持っている必要はありません(APIキーを使って匿名で読み込みます)。

## 7. 他アプリの「共有」からリンクを受け取る機能について

YouTubeや楽譜購入サイトなどで共有ボタンを押したときに「うたコレ」を共有先として選べる機能です
(Web Share Target APIという標準機能を使用)。設定不要ですぐ使えますが、次の制約があります。

- **ホーム画面に追加(PWAインストール)していないと共有先の一覧に出てきません**
- **Android(Chrome等)のみ対応です。iPhoneのSafari/PWAは、この機能自体に対応していないため
  共有先には出てきません**(iOS側の仕様であり、アプリ側の対応では解決できません)
- manifest.jsonを更新した後、既にホーム画面に追加済みの場合は、一度アンインストールして
  入れ直すと確実に反映されます

共有されたリンクを受け取ると、動画URLか楽譜入手先かを選んだ上で、
「既存の曲を選んで上書き」または「このリンクで新規に曲を登録」を選べる画面が開きます。

## 8. 広告表示について

### 8-1. 表示ルール

- **曲の新規登録**(編集は対象外): アカウント作成から7日間は広告なし。8日目以降は、
  通算の新規登録回数が **1・6・11・16・21回目...(5件おき)** になるたびに15秒の広告を挟む。
  ただし**1日に5回広告を見たら、その日はもう広告を挟まず登録できる**(翌日リセット)
- **CSV一括登録**: 上記のルールとは関係なく、**毎回**アップロード前に30秒の広告を挟む
  (7日間の猶予・1日5回の上限、いずれも対象外)

登録回数・広告視聴回数はこの端末に永続化されます(曲を削除しても登録回数は減りません)。

### 8-2. 広告本体について

`AdGateModal`(`src/App.jsx`内)は、**Google Publisher Tag(GPT)のテスト用広告ユニット
(`/6355419/Travel`)** を表示します。これはGoogleの公式ドキュメントに掲載されている
動作確認用のユニットで、実際の収益は発生しません。`index.html`でGPTライブラリ
(`https://securepubads.g.doubleclick.net/tag/js/gpt.js`)を読み込んでいます。

本番で収益化する場合は、`src/App.jsx`内の `GPT_AD_UNIT_PATH` を、
ご自身のGoogle Ad Manager上で作成した広告ユニットのパス(例: `/1234567/自分のサイト名`)に
書き換えてください。Ad Managerのアカウント作成・広告ユニットの作成・審査は別途必要です。

なお、GPTの通常のディスプレイ広告自体には「指定秒数見るまで閉じられない」という機能はないため、
`AdGateModal`側のタイマーで「続ける」ボタンを一定時間無効化する形で強制視聴を実現しています。
より本格的な動画リワード広告(視聴完了を広告側で検知する形式)にしたい場合は、
Google Ad Managerの動画広告ユニット、またはネイティブアプリ化した際のAdMobリワード広告への
置き換えを検討してください。

## 9. 公開後の確認

- スマホでURLを開き、「ホーム画面に追加」からアプリのようにインストールできるか確認する
- 一度データを登録したあと、ページを再読み込みしてもデータが残っているか確認する
  (IndexedDBに保存されているため、同じブラウザ・同じ端末であれば保持されます)

## 補足: publicフォルダについて

`manifest.json`・`service-worker.js`・`icons/`は`public/`フォルダの中に置いてあります。
Viteは`public/`の中身をそのまま`dist`にコピーする仕様のため、この場所に置く必要があります
(実際にこの環境でビルドを検証し、`public/`に置かないとPWA関連ファイルが`dist`に含まれない
ことを確認した上でこの構成にしています)。

## 注意点

- データは基本的に**この端末のブラウザの中(IndexedDB)**に保存されます。
  Googleログインを設定・利用した場合は、そのユーザー自身のGoogleドライブ(アプリ専用の
  隠し領域)にも保存され、同じGoogleアカウントでログインすれば他の端末からも同じデータに
  アクセスできます。
- 他の人と曲やプロフィールを共有したい場合は、Driveの共有機能ではなく、
  アプリ内の「曲リストを共有」機能(QRコード/URL)や、CSV/JSONのバックアップ機能を使ってください。
- ブラウザの「サイトデータを消去」や「シークレットモード」では、ログインしていない場合の
  ローカルデータは保持されません(Googleログイン済みの場合はドライブ側にデータが残ります)。

